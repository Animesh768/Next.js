// 'use client' must be at the top of the file, before any imports
'use client';

import React from 'react';

const Page = () => {
  return (
    <div className="min-h-screen bg-gradient-to-l from-indigo-600 via-purple-500 to-pink-600 flex items-center justify-center">
      <div className="text-white text-4xl text-right">
        Career
      </div>
    </div>
  );
};

export default Page;
